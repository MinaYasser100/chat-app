import 'package:flutter/material.dart';

class EditInfoBodyView extends StatelessWidget {
  const EditInfoBodyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
